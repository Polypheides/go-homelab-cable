self.addEventListener('install', (e) => {
    // Dummy install event to pass PWA requirements
});

self.addEventListener('fetch', (e) => {
    // Dummy fetch event to pass PWA requirements.
    // In a real offline app, we would cache assets here.
});
